{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'No build step: Vercel bundles src/server.ts itself. Types are checked by npm run typecheck locally and in the Docker image.'"
}
